{
  "packages": {
    "python": "3.12.8"
  },
  "steps": [
    {
      "name": "install",
      "commands": [
        "python -m venv /app/.venv",
        "pip install --upgrade pip",
        "pip install aiogram python-dotenv aiofiles reportlab openai sqlalchemy"
      ]
    }
  ],
  "deploy": {
    "command": "python bot2.py"
  }
}
